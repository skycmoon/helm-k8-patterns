apiVersion: v1
kind: Service
metadata:
  name: {{.Chart.Name}}
  namespace: {{.Values.namespace}}
  labels:
    app: {{.Chart.Name}}
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: {{.Chart.Name}}
  sessionAffinity: None
  type: ClusterIP