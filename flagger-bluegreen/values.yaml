version: latest
namespace: barge
environment: test
region: us-west-2
clusterName: eks-dev-compute

numreplicas: 1
maxreplicas: 5
maxutilization: 85

# polling interval * polling count = total time to hold canary before rolling back.
releaseApprovalPollingInterval: 1m
releaseApprovalPollingCount: 2880
progressDeadlineInSeconds: 172800 # 48 hours
